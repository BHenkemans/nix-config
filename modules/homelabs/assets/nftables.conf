table inet filter {
  chain input {
    type filter hook input priority filter; policy drop;

    iif "lo" accept

    ct state established,related accept
    ct state invalid drop

    # Allow container DNS queries to reach aardvark-dns on the host
    ip saddr 10.89.0.0/16 meta l4proto { tcp, udp } th dport 53 accept

    # Traefik (HTTP/HTTPS) is LAN-only — external traffic reaches services
    # through Cloudflare tunnels, never through these host ports.
    ip saddr { 192.168.0.0/16, 10.0.0.0/8 } tcp dport { 80, 443 } accept

    ip saddr 192.168.0.0/16 tcp dport 22 accept
    ip saddr 10.0.0.0/8 tcp dport 22 accept

    icmp type echo-request accept
    icmpv6 type { echo-request, nd-neighbor-solicit, nd-router-advert, nd-neighbor-advert } accept
  }

  chain forward {
    type filter hook forward priority filter; policy accept;
  }

  chain output {
    type filter hook output priority filter; policy accept;
  }
}
